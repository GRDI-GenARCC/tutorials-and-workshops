# Syntax error (comment this out so next block of code can run)
print("Hello, World!"

# Runtime error (comment this out so next block of code can run)
x = 10
y = 0
result = x / y  # Division by zero

# Logical error
radius = 5
area = 2 * 3.14 * radius  # Incorrect formula for calculating the area of a circle

# Try-Except
try:
    result = 10 / 0
except Exception as e:
    print("An error occurred:", str(e))
